{
  "name": "frameworkcypress",
  "version": "1.0.0",
  "description": "Framework de test cypress",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "runChrome": "npx cypress run --browser chrome --headed",
    "merge-json": "npx mochawesome-merge cypress/reports/*.json -o cypress/reports/merged-report.json",
    "generate-html": "npx marge cypress/reports/merged-report.json -o cypress/reports/merged-html-report -f merged-report.html",
    "report": "npm run merge-json && npm run generate-html"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^15.13.1",
    "mochawesome": "^7.1.4"
  }
}
